#ifndef PRIMES_H
#define PRIMES_H

#include <stdio.h>

void find_primes(unsigned long long start_x, unsigned long long end_x, char verbose);
int is_prime(unsigned long long *z);

#endif // PRIMETEST_H

